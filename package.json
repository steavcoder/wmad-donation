{
  "name": "wmad-donate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "prisma generate",
    "clean": "rm -rf .next",
    "dev": "env -u CI next dev --webpack",
    "dev:clean": "npm run clean && env -u CI next dev --webpack",
    "dev:turbo": "env -u CI next dev",
    "build": "prisma generate && next build",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:baseline:mark-all-applied": "prisma migrate resolve --applied 20260420135929_init_postgres && prisma migrate resolve --applied 20260420145147_add_donation_payment_fields && prisma migrate resolve --applied 20260421021500_add_user_profile_major && prisma migrate resolve --applied 20260421021821_add_user_profile_major && prisma migrate resolve --applied 20260421110000_add_donation_status && prisma migrate resolve --applied 20260422120000_add_user_status_disabled",
    "build:with-migrate": "prisma generate && prisma migrate deploy && next build",
    "start": "next start",
    "lint": "eslint",
    "admin:create": "node scripts/create-admin.mjs",
    "member:create": "node scripts/create-member.mjs",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "bcryptjs": "^3.0.2",
    "jsonwebtoken": "^9.0.2",
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "prisma": "^6.18.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
